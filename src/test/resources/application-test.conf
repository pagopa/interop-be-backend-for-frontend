backend-for-frontend {
  port = 8088
  rsa-private-path = "rsa-private-path"

  jwt {
    issuer = "issuer"
    audience = [ "test.interop.pagopa.it" ]
    duration-seconds = 60
  }
}

interop-commons {
  jwt {
    public-keys {
      url = "well-known"
    }
  }
}
